import { SiteIcon } from './SiteIcon';

export interface FeatureIconCollection {
    actuals: SiteIcon;
    about: SiteIcon;
    events: SiteIcon;
    donation: SiteIcon;
    contact: SiteIcon;
    downloads: SiteIcon;
    media: SiteIcon;
    classes: SiteIcon;
    publications: SiteIcon;
    webshop: SiteIcon;
}
